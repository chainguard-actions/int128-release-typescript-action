name: Test dry-run on tag push
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_REF: refs/tags/v1.0.0
      GITHUB_EVENT_NAME: push
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Run action in dry-run mode on tag push (expect failure at API level)
        id: action
        uses: .
        with:
          major-version: '1'
          increment-level: 'minor'
          dry-run: 'true'
          token: 'fake-token'
        continue-on-error: true

      - name: Assert action failed (expected - no real GitHub token available)
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          # On a tag push, the action follows up the current tag.
          # It should fail because it cannot authenticate with GitHub API
          # using a fake token, but it should NOT fail due to input validation.
          if [ "$OUTCOME" != "failure" ]; then
            echo "FAIL: expected action to fail (no real token), but got outcome=$OUTCOME"
            exit 1
          fi
          echo "PASS: action correctly detected tag push context and failed at API/network level"
